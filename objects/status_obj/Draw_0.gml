/// @description Insert description here

draw_self();

/**
draw_sprite(shine_status, current_frame, self.x, self.y);
if(current_frame == frame_count){
	current_frame = 0;
}else{
	current_frame += 1;
}
**/

draw_text(x-15, y-10, face_text);

